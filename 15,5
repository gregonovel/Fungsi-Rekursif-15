def kombinasi(n, r):
    # Base Cases
    if r == 0 or r == n:
        return 1
    # Recursive Case (Menggunakan sifat Segitiga Pascal)
    return kombinasi(n - 1, r - 1) + kombinasi(n - 1, r)

print(kombinasi(5, 2))  # Output: 10
